# Python
__pycache__/
*.pyc
.venv/
.venv.*/

# Environment / secrets
.env
local_server.token

# macOS
.DS_Store

# Logs
*.log

# Runtime/user data (personal conversation history, indexes, permissions)
memory/

# Compiled/codesigned Photos-/Kamera-Helfer (data_root()-Dev-Fallback landet im
# Repo-Root, wenn photos_client.py/camera_client.py ausserhalb der gebauten App
# laeuft - siehe app/data_dir.py)
PhotosHelper/
CameraHelper/

# Editor / tooling caches
.cache/
.vscode/

# Swift / Xcode
JarvisApp/.build/
JarvisApp/.swiftpm/
**/DerivedData/
*.xcuserstate
**/xcuserdata/

# Voice recordings / audio test fixtures (biometric, never belongs in git)
*.wav
*.mp3

# Old manual backup dumps and large export archives
/ Jarvis App 1/
*.zip

# Bundled Ollama runtime + model weights (multi-GB binaries, not fit for git -
# assembled locally before an export/build). Licenses/ stays tracked (small
# text files).
JarvisApp/Sources/JarvisApp/Resources/ollama-runtime/
JarvisApp/Sources/JarvisApp/Resources/bundled-models/
